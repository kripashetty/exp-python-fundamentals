- Common built-in iterable types
    - Objects that are sequences
        - List 
        - Tuple 
        - Strings
    - Objects that are collections
        - Dictionary 
        - Sets

Iterato PRotocol 
- Iterator object iterates over a iterable object
- iter() - get the iterable calling __iter__, next() - get the next item calling __next__
- for calls iter and then next (i.e follows the ierator protocol)