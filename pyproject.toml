[project]
name = "exp-asynchrony-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.14.1",
    "pydantic[email]>=2.13.4",
    "pydantic-settings>=2.14.2",
]

[tool.uv.workspace]
members = [
    "pydantic/pydantic-ai",
    "fast-api/awesome-project",
]
